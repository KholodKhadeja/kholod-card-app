<!-- import React, { Fragment } from 'react';
import "./TopBarComponent.scss";
import ListItemForTopBar from 'components/listItemForTopBar';
import { Link } from 'react-router-dom';
import ListItemComponent from './listItemComponent';

let unLoggedUser={
  leftList:[
{
  label:"Home",
  url:"/",
},
{
  label:"About Us",
  url:"/",
}], 
rightList:[
  {
    label:"Login",
    url:"/",
  },
  {
    label:"Register",
    url:"/",
  },
  {
    label:"For Busniss",
    url:"/",
  }
], 
}

let LoggedInUser={
  leftList:[
{
  label:"Home",
  url:"/",
},
{
  label:"My Cards", /*for busniss show the button of add*/
  url:"/",
},
{
  label:"About Us",
  url:"/",
}], 
rightList:[
  {
    label:"User name",
    url:"/",
  },
  {
    label:"Logout",
    url:"/",
  }
], 
}
const TopBarComponent = () => {
    return (
    <Fragment>
    <nav className="navbar navbar-expand-lg navbar-light bg-light">
  <div className="container-fluid">
    <a className="navbar-brand">
        <img src="https://github.com/KholodKhadeja/logoImage/blob/main/logoImg.png?raw=true"  width="90"
         height="50"/>
        </a>
    <div className="collapse navbar-collapse" id="navbarNav">
      <ul className="navbar-nav me-auto mb-2 mb-lg-0">
        {/*will add the other list when adding the redux and loggedIn*/}
        {unLoggedUser.leftList.map((item,idx)=>(
          <ListItemComponent  label={item.label} url={item.url} dir="left" key={idx+"list2"}/>
        ))
        }
      </ul>

      <span className="navbar-text">
      {unLoggedUser.rightList.map((item,idx)=>(
          <ListItemComponent  label={item.label} url={item.url} dir="right" key={idx+"list"}/>
        ))
        }
      </span>
    </div>
  </div>
</nav>
</Fragment>
    );
}

export default TopBarComponent; -->
